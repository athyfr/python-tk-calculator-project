# rumdl configuration file

# Global configuration options
[global]
# List of rules to disable (uncomment and modify as needed)
# disable = ["MD013", "MD033"]

# List of rules to enable exclusively (if provided, only these rules will run)
# enable = ["MD001", "MD003", "MD004"]

# List of file/directory patterns to include for linting (if provided, only these will be linted)
# include = [
#    "docs/*.md",
#    "src/**/*.md",
#    "README.md"
# ]

# List of file/directory patterns to exclude from linting
exclude = [
    # Common directories to exclude
    ".git",
    ".github"
]

# Respect .gitignore files when scanning directories (default: true)
respect-gitignore = true

# Markdown flavour/dialect 
flavor = "gfm" # GitHub flavour Markdown.

# Rule-specific configurations (uncomment and modify as needed)

[MD003]
style = "atx"  # Heading style (atx uses hashtags to mark headers)

[MD004]
style = "dash"  # Use dashes (-) to mark list items

[MD007]
indent = 4  # Indent list items by 4 spaces.

[MD013]
line-length = 80    # Set line length to standard limit
code-blocks = false # Exclude code blocks from line length check
tables = true       # Include tables in line length check
headings = true     # Include headings in line length check

[MD033]
allowed-elements = ["details", "summary"] # Allow collapsible HTML sections.

[MD044]
names = ["rumdl", "Markdown", "GitHub"] # Proper names that should be capitalized correctly
code-blocks = false # Don't check code blocks for proper names
